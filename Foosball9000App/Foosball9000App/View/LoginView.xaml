<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Foosball9000App.Viewmodels;assembly=Foosball9000App"
             x:Class="Foosball9000App.View.LoginView">
  <ContentPage.BindingContext>
    <viewmodels:LoginViewModel/>
  </ContentPage.BindingContext>
  <StackLayout>
    <ActivityIndicator Color="Black" IsRunning="{Binding IsBusy}" />
    
    <Label Text="Login" VerticalOptions="Center" HorizontalOptions="Center" />
    
    <Entry Text="{Binding Username}" />
    <Entry Text="{Binding Password}" IsPassword="True" />
    
    <Button Text="LoginView" Command="{Binding LoginCommand}" />
  </StackLayout>
</ContentPage>